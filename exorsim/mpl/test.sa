TEST: PROC OPTIONS(MAIN)
  DCL HELLO CHAR(100) INIT('HELLO, WORLD!!')
  DCL CR CHAR INIT(13)
  DCL NN BIN(2)
  DCL STR CHAR(4)
  DCL CR1 CHAR INIT(13)

  CALL PUSH2<1,2,10>
  CALL PUSH2<3,4,20>
  CALL PUSH2<5,6,40>

  CALL PULL2 GIVING<,,NN>
  STR = NN
  CALL DSPLY<1,1,ADDR(STR)>


  CALL PULL2 GIVING<,,NN>
  STR = NN
  CALL DSPLY<1,1,ADDR(STR)>

  CALL PULL2 GIVING<,,NN>
  STR = NN
  CALL DSPLY<1,1,ADDR(STR)>

  CALL DSPLY<1,1,ADDR(HELLO)>
  CALL DSPLY<1,1,ADDR(STR)>
  CALL MDOS
  END
